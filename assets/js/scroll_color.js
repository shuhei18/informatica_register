window.addEventListener("scroll", function() {

    let scroll = window.pageYOffset;
  
    if (scroll > 100) {
    document.getElementById("scroll-color").style.backgroundColor = 'hsla(0,0%,88.6%,0.45)';
    }
  
  });

  
